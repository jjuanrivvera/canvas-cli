---
title: canvas assignment-groups delete
---

## canvas assignment-groups delete

Delete an assignment group

### Synopsis

Delete an assignment group.

You can optionally move assignments to another group before deleting.

Examples:
  canvas assignment-groups delete 456 --course-id 123
  canvas assignment-groups delete 456 --course-id 123 --force
  canvas assignment-groups delete 456 --course-id 123 --move-to 789

```
canvas assignment-groups delete <group-id> [flags]
```

### Options

```
      --course-id int   Course ID (required)
      --force           Skip confirmation prompt
  -h, --help            help for delete
      --move-to int     Move assignments to another group before deleting
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas assignment-groups](canvas_assignment-groups.md)	 - Manage Canvas assignment groups

###### Auto generated by spf13/cobra on 14-Jan-2026
