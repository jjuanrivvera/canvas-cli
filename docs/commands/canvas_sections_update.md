---
title: canvas sections update
---

## canvas sections update

Update a section

### Synopsis

Update an existing section.

Examples:

```bash
canvas sections update 456 --name "Updated Section Name"
canvas sections update 456 --start-at "2024-02-01"
canvas sections update 456 --restrict-dates
```

```
canvas sections update <section-id> [flags]
```

### Options

```
      --end-at string             Section end date (ISO 8601)
  -h, --help                      help for update
      --integration-id string     Integration ID
      --name string               Section name
      --override-sis-stickiness   Override SIS stickiness
      --restrict-dates            Restrict enrollments to section dates
      --sis-section-id string     SIS section ID
      --start-at string           Section start date (ISO 8601)
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --dry-run           Print curl commands instead of executing requests
      --instance string   Canvas instance URL (overrides config)
      --limit int         Limit number of results for list operations (0 = unlimited)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
      --show-token        Show actual token in dry-run output (default: redacted)
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas sections](canvas_sections.md)	 - Manage Canvas course sections

###### Auto generated by spf13/cobra on 24-Jan-2026
