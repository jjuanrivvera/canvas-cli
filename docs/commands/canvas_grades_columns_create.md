---
title: canvas grades columns create
---

## canvas grades columns create

Create a custom column

### Synopsis

Create a new custom gradebook column.

Examples:

```bash
canvas grades columns create --course-id 123 --title "Notes"
canvas grades columns create --course-id 123 --title "Attendance" --teacher-notes
```

```
canvas grades columns create [flags]
```

### Options

```
      --course-id int   Course ID (required)
  -h, --help            help for create
      --hidden          Hide column
      --position int    Column position
      --read-only       Read-only column
      --teacher-notes   Teacher notes column
      --title string    Column title (required)
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas grades columns](canvas_grades_columns.md)	 - Manage custom gradebook columns

###### Auto generated by spf13/cobra on 14-Jan-2026
