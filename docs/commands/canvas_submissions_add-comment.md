---
title: canvas submissions add-comment
---

## canvas submissions add-comment

Add a comment to a submission

### Synopsis

Add a comment to a specific submission.

Examples:

```bash
canvas submissions add-comment --course-id 123 --assignment-id 456 --user-id 789 --text "Great work!"
canvas submissions add-comment --course-id 123 --assignment-id 456 --user-id 789 --text "Feedback" --group
```

```
canvas submissions add-comment [flags]
```

### Options

```
      --assignment-id int   Assignment ID (required)
      --course-id int       Course ID (required)
      --group               Share comment with group members
  -h, --help                help for add-comment
      --text string         Comment text (required)
      --user-id int         User ID (required)
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas submissions](canvas_submissions.md)	 - Manage Canvas submissions

###### Auto generated by spf13/cobra on 14-Jan-2026
