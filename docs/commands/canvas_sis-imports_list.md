---
title: canvas sis-imports list
---

## canvas sis-imports list

List SIS imports

### Synopsis

List SIS imports for an account.

Workflow states: initializing, created, importing, cleanup_batch,
                 imported, imported_with_messages, aborted,
                 failed, failed_with_messages, restoring, partially_restored

Examples:
  canvas sis-imports list --account-id 1
  canvas sis-imports list --account-id 1 --workflow-state imported

```
canvas sis-imports list [flags]
```

### Options

```
      --account-id int          Account ID (required)
      --created-before string   Filter imports created before date (ISO8601)
      --created-since string    Filter imports created since date (ISO8601)
  -h, --help                    help for list
      --workflow-state string   Filter by workflow state
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas sis-imports](canvas_sis-imports.md)	 - Manage SIS imports

###### Auto generated by spf13/cobra on 14-Jan-2026
