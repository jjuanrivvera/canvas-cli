---
title: canvas courses create
---

## canvas courses create

Create a new course

### Synopsis

Create a new course in an account.

Examples:
  canvas courses create --account-id 1 --name "Introduction to Programming"
  canvas courses create --account-id 1 --name "Biology 101" --code "BIO101" --term 5
  canvas courses create --account-id 1 --name "Math 201" --start-at "2024-09-01" --end-at "2024-12-15"
  canvas courses create --account-id 1 --name "Public Course" --public --offer

```
canvas courses create [flags]
```

### Options

```
      --account-id int         Account ID to create course in (required)
      --code string            Course code
      --default-view string    Default view (feed, wiki, modules, syllabus, assignments)
      --end-at string          End date (ISO 8601)
  -h, --help                   help for create
      --license string         Course license
      --name string            Course name (required)
      --offer                  Publish course immediately
      --public                 Make course public
      --sis-course-id string   SIS course ID
      --start-at string        Start date (ISO 8601)
      --term int               Enrollment term ID
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas courses](canvas_courses.md)	 - Manage Canvas courses

###### Auto generated by spf13/cobra on 14-Jan-2026
