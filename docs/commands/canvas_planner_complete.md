---
title: canvas planner complete
---

## canvas planner complete

Mark an item as complete

### Synopsis

Mark a planner item as complete by creating an override.

Type can be: Assignment, Quiz, DiscussionTopic, WikiPage, CalendarEvent

Examples:
  canvas planner complete Assignment 123
  canvas planner complete Quiz 456

```
canvas planner complete <type> <id> [flags]
```

### Options

```
  -h, --help   help for complete
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas planner](canvas_planner.md)	 - Manage Canvas planner items and notes

###### Auto generated by spf13/cobra on 13-Jan-2026
