---
title: canvas accounts sub
---

## canvas accounts sub

List sub-accounts of an account

### Synopsis

List sub-accounts for a given parent account.

Use --recursive to get the entire account tree.

Examples:

```bash
canvas accounts sub 1
canvas accounts sub 1 --recursive
```

```
canvas accounts sub <account-id> [flags]
```

### Options

```
  -h, --help        help for sub
      --recursive   List entire account tree recursively
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --dry-run           Print curl commands instead of executing requests
      --instance string   Canvas instance URL (overrides config)
      --limit int         Limit number of results for list operations (0 = unlimited)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
      --show-token        Show actual token in dry-run output (default: redacted)
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas accounts](canvas_accounts.md)	 - Manage Canvas accounts

###### Auto generated by spf13/cobra on 24-Jan-2026
