---
title: canvas groups categories create
---

## canvas groups categories create

Create a group category

### Synopsis

Create a new group category in a course or account.

Examples:

```bash
canvas groups categories create --course-id 123 --name "Project Teams"
canvas groups categories create --account-id 1 --name "Clubs" --self-signup enabled
```

```
canvas groups categories create [flags]
```

### Options

```
      --account-id int           Account ID
      --auto-leader string       Auto leader (first, random)
      --course-id int            Course ID
      --create-group-count int   Number of groups to create
      --group-limit int          Group member limit
  -h, --help                     help for create
      --name string              Category name (required)
      --self-signup string       Self signup (enabled, restricted)
      --sis-category-id string   SIS category ID
      --split-group-count int    Number of groups to split students into
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --dry-run           Print curl commands instead of executing requests
      --instance string   Canvas instance URL (overrides config)
      --limit int         Limit number of results for list operations (0 = unlimited)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
      --show-token        Show actual token in dry-run output (default: redacted)
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas groups categories](canvas_groups_categories.md)	 - Manage group categories

###### Auto generated by spf13/cobra on 24-Jan-2026
