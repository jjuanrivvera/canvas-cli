---
title: canvas discussions list
---

## canvas discussions list

List discussion topics in a course

### Synopsis

List all discussion topics in a Canvas course.

Examples:

```bash
canvas discussions list --course-id 123
canvas discussions list --course-id 123 --order-by recent_activity
canvas discussions list --course-id 123 --scope pinned
canvas discussions list --course-id 123 --filter unread
```

```
canvas discussions list [flags]
```

### Options

```
      --announcements     Only show announcements
      --course-id int     Course ID (required)
      --filter string     Filter by: all, unread
  -h, --help              help for list
      --include strings   Include: all_dates, sections, sections_user_count, overrides
      --order-by string   Order by: position, recent_activity, title
      --scope string      Scope: locked, unlocked, pinned, unpinned
      --search string     Search term
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas discussions](canvas_discussions.md)	 - Manage Canvas discussion topics

###### Auto generated by spf13/cobra on 14-Jan-2026
