---
title: canvas assignment-groups create
---

## canvas assignment-groups create

Create a new assignment group

### Synopsis

Create a new assignment group in a course.

Examples:
  canvas assignment-groups create --course-id 123 --name "Homework"
  canvas assignment-groups create --course-id 123 --name "Exams" --weight 40 --position 2
  canvas assignment-groups create --course-id 123 --name "Quizzes" --weight 20 --drop-lowest 1

```
canvas assignment-groups create [flags]
```

### Options

```
      --course-id int      Course ID (required)
      --drop-highest int   Number of highest scores to drop
      --drop-lowest int    Number of lowest scores to drop
  -h, --help               help for create
      --name string        Group name (required)
      --position int       Position in course
      --weight float       Group weight percentage (0-100)
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas assignment-groups](canvas_assignment-groups.md)	 - Manage Canvas assignment groups

###### Auto generated by spf13/cobra on 14-Jan-2026
