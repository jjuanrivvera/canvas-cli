---
title: canvas overrides update
---

## canvas overrides update

Update an override

### Synopsis

Update an existing assignment override.

Examples:

```bash
canvas overrides update 789 --course-id 123 --assignment-id 456 --due-at "2024-03-18T23:59:00Z"
canvas overrides update 789 --course-id 123 --assignment-id 456 --title "New title"
```

```
canvas overrides update <override-id> [flags]
```

### Options

```
      --assignment-id int    Assignment ID (required)
      --course-id int        Course ID (required)
      --due-at string        Due date (ISO 8601)
  -h, --help                 help for update
      --lock-at string       Lock date (ISO 8601)
      --student-ids string   Comma-separated student IDs
      --title string         Override title
      --unlock-at string     Unlock date (ISO 8601)
```

### Options inherited from parent commands

```
      --as-user int       Masquerade as another user (admin feature, requires permission)
      --config string     config file (default is $HOME/.canvas-cli/config.yaml)
      --instance string   Canvas instance URL (overrides config)
      --no-cache          Disable caching of API responses
  -o, --output string     Output format: table, json, yaml, csv (default "table")
  -v, --verbose           Enable verbose output
```

### SEE ALSO

* [canvas overrides](canvas_overrides.md)	 - Manage Canvas assignment overrides

###### Auto generated by spf13/cobra on 14-Jan-2026
