run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    # Default linters
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    # Formatting
    - gofmt
    - goimports
    - misspell
    # Error handling
    - errcheck
    - bodyclose
    # Code quality
    - goconst
    - unconvert
    # Security
    - gosec

linters-settings:
  gofmt:
    simplify: true

  goimports:
    local-prefixes: github.com/jjuanrivvera/canvas-cli

  errcheck:
    exclude-functions:
      - (io.Closer).Close

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  exclude-rules:
    # Exclude errcheck in test files - test error handling is often intentionally simplified
    - path: _test\.go
      linters:
        - errcheck

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true
